# ADR 0001: One shared multi-brand application

Status: accepted.

VanAssist, TowSmart and TrailerWise use one repository, runtime and shared
platform with trusted-host brand resolution. They retain separate domains,
presentation, SEO, feature/module configuration and data scope. This avoids
three copied applications while preserving product boundaries. NDTFlow and
SignConsole remain separate.

