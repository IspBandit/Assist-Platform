# Platform data_catalogue pack

Portable acquisition index mirrored from Assist RIC for DATA-011A.

- Catalogue SoR remains `government_datasets` (migration 123 seeds children/themes/paid stubs).
- Raw bulk files are not committed; download with:

```bash
python scripts/data_catalogue/download_datasets.py
```

Assist RIC is the acquisition engine. See docs/RIC_DATASET_AVAILABILITY.md.
